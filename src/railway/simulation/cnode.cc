#include "cnode.h"

using namespace ns3;


CNode::CNode()
{
    // TypeId tid = TypeId::LookupByName("ns3::UdpSocketFactory");
    // m_socket = Socket::CreateSocket(this, tid);
    // InetSocketAddress local = InetSocketAddress(Ipv4Address::GetAny(), LAN_PORT);
    // m_socket->Bind(local);
    // m_socket->SetRecvCallback(MakeCallback(&PennSearch::RecvMessage, this));
}
